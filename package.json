{
  "name": "project4",
  "version": "2.4.0-next.0",
  "engines": {
    "node": ">=20"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "powershell Remove-Item reports,evidence,test-results,dashboard,playwright-report,allure-results -Recurse -Force -ErrorAction SilentlyContinue",
    "test": "playwright test",
    "test:framework": "playwright test tests/framework --workers=1",
    "test:history": "playwright test tests/framework/history-manager.spec.js tests/framework/history-blockers.spec.js --workers=1",
    "test:clean": "npm run clean && playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.3",
    "allure-playwright": "^3.10.0"
  },
  "dependencies": {
    "pdfkit": "^0.19.1"
  }
}
